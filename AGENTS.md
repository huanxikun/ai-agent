# Repository workflow

- After completing requested code changes and running relevant verification, create a Git commit with a clear Conventional Commit message.
- Never commit secrets, `.env`, build outputs, logs, or untracked IDE-specific configuration.
- Report the commit hash and verification result when handing off the change.
